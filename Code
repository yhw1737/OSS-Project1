#include <stdio.h>

int main(){
char a;
  char world[100];
  gets(world);
  for(int i=0;i<5;i++){
    printf("hello !\n");
    printf("%s", world);
  }
  printf("한 번더 출력할까요? >> "); // yes = 0, no = 1
    scanf_s("%d", &n);

    if (n == 0)
    {
        for (int i = 0; i < 5; i++) {
            printf("hello !\n");
            printf("%s", world);
        }
    }
    else if (n == 1)
        return 0;
}
